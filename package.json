{
  "name": "violet-rates-app",
  "version": "1.0.0",
  "license": "MIT",
  "author": "Omar Niazi",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "mongodb": "^4.7.0",
    "mongoose": "^6.4.0"
  },
  "scripts": {
      "start": "node server.js",
      "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"
  },
  "engines": {
      "node": "16.5.1"
  }
}